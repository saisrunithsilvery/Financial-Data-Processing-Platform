CREATE OR REPLACE TABLE PRE (
     adsh VARCHAR(50),
    report INT,
    line INT,
    stmt VARCHAR(5),
    inpth INT,
    rfile VARCHAR(5),
    tag VARCHAR(255),
    version VARCHAR(50),
    plabel TEXT,
    negating INT
);